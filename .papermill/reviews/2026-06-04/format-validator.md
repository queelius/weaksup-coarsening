# Format Validator Report

**Date**: 2026-06-04
**Focus**: Build verification, citation/label resolution, page count, LaTeX
warnings, and confirmation that the T4 rewrite did not break cross-references or
introduce overfull boxes. Confirm no em-dash (U+2014) anywhere.

## Build

- `make clean && make paper` (pdflatex -> bibtex -> pdflatex x3) completes with
  **exit 0**. Output: `main.pdf`, **20 pages**, 421,284 bytes.
- This is a fresh build from a cleaned tree (the `.bbl` was deleted by
  `make clean` and regenerated by bibtex this pass), so the artifacts on disk are
  current with the source, not stale.

## Citation and cross-reference resolution

- **0 undefined citations** and **0 undefined references** in `main.log`
  (explicit grep for "undefined citation/reference/control sequence" returns
  nothing).
- **0 multiply-defined labels.**
- `main.aux` issues 28 `\citation` keys; `main.bbl` contains 28 `\bibitem`
  entries; the two sets are equal. The bibliography is complete.
- `pdftotext main.pdf` contains **no `??`** (no unresolved `\ref`/`\cref`), so
  every cleveref cross-reference (theorems, remarks, equations, figures,
  conditions, tables) resolved. I specifically confirmed the T4-area labels
  resolve and are referenced consistently: `thm:goldset` (cited from
  methodology, validation, discussion), `rmk:r-definition`, `rmk:l2-loss`,
  `rmk:r-dependence`, `eq:goldset-bound`, `eq:goldset-lower`, `eq:trace-bound`,
  `fig:goldset`. The T4 rewrite did **not** break any cross-reference.

## Overfull / underfull boxes

- **Overfull \hbox: 2**, both tiny:
  - 6.62572pt too wide at `methodology.tex` lines 200-209 (the `rmk:l2-loss`
    display-adjacent paragraph with the logit/`w(a)` inline math).
  - 0.75015pt too wide at `translation.tex` lines 113-117.
- **Underfull \hbox: 0. Overfull \vbox: 0.**
- The 6.6pt overflow is the only one a reader could conceivably notice (a hair
  into the margin); 0.75pt is invisible. Neither was introduced by the T4 rewrite
  in a way that harms the result; both are trivial. FIX (optional): a `\\` or
  minor rephrase in the `rmk:l2-loss` sentence. Severity: minor/cosmetic.

## LaTeX warnings

- 4x `Package hyperref Warning: Token not allowed in a PDF string (Unicode)`.
  Source: the title/author block (the `\\` line break and the ORCID `\href` in
  the `\author` field) being flattened into PDF bookmark strings. These affect
  only the PDF outline metadata, **not body text or links**. Severity:
  cosmetic. FIX (optional): wrap the offending tokens in `\texorpdfstring{}{}`.
- No other LaTeX/package/font warnings of substance.

## bibtex warnings

3 cosmetic warnings in `main.blg` (empty booktitle in `anandkumar2014tensor`;
volume+number both set in `anandkumar2014tensor` and `ratner2019training`). Do
not affect typeset output. Detailed in the citation-verifier report.

## Em-dash (U+2014) and stray-unicode check

- **0 literal U+2014 (em-dash)** across `main.tex`, all of `sections/`, and
  `refs.bib`. Also **0 in the generated `main.bbl`.** The repository convention
  is satisfied.
- **0 stray U+2013 / U+2012 / U+2015** (en-dash and relatives) in source. The
  `--` sequences in the source are LaTeX-encoded dashes that typeset correctly;
  they are not literal Unicode dashes.

## Venue / format notes

- Document class `article`, 11pt, letterpaper, 1in margins, `plainnat` + natbib
  + cleveref. Self-described target "12 pages incl. references" (main.tex
  header); the current build is 20 pages. This is a **content/length** matter for
  the chosen venue, not a build defect, but flagged: if the true target is a
  12-page conference limit, the paper is over by a wide margin and will need
  condensation or a different (journal/extended) target. Severity: major IF the
  12-page limit is binding; otherwise N/A. (No venue is fixed in a state file, so
  I cannot resolve which; carrying it as a venue-conditional flag.)

## Format-validator confidence

HIGH. Clean build, 0 undefined citations, 0 broken cross-references, 0 U+2014,
only 2 trivial overfull boxes and 4 cosmetic hyperref bookmark warnings. The T4
rewrite is production-clean. The only non-cosmetic flag is venue-conditional:
the 20-page length versus the header's stated 12-page target.
